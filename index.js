// Performing a task
const greet = (firstName, lastName) => {
  console.log(`Hello, ${firstName} ${lastName}!`);
};

// Calculating a value
const square = (number) => {
  return number * number;
};

let number = square(2);
console.log(number);
